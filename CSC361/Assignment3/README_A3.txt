Assumptions: 
we are not to include any other IP headers than ICMP or UDP in “the protocol field of IP headers” output. As we are supposed to ignore any other IP protocols


How to run:
-Make sure you have the file, traceroute_analyzer.py, and packet.py in the same directory
-then in the command line: python3 traceroute_analyzer.py filename.pcap