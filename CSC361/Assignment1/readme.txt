This program is meant to be run in the terminal the following way on the command line
python3 SmartClient.py uvic.ca

This program is designed to only work for .ca, .com, .net, and .org websites.
others could easily be added.

This program also does not work properly on websites that do not allow
the HEAD HTTP request, eg applemusic.com.
however, the error is handled